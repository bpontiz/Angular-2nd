import { HoverBgColorDirective } from './hover-bg-color.directive';

describe('HoverBgColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverBgColorDirective();
    expect(directive).toBeTruthy();
  });
});
